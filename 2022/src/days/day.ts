import { DayAbstract } from '../utils/day';

type Input = any;

export class Day extends DayAbstract {
    parse(input: string): Input {
        return '';
    }

    partOne(): number {
        return 0;
    }

    partTwo(): number {
        return 0;
    }
}
